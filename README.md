# E-commerce Store Database (DBMS Assignment)

## 📌 Overview
This project implements a relational database for an **E-commerce Store** using **MySQL**.  
It covers entities such as Customers, Products, Orders, Payments, and Reviews,  
and demonstrates **relationships** (One-to-Many, Many-to-Many).

---

## 🚀 How to Run

### 1. Clone the repository
```bash
git clone https://github.com/<your-username>/dbms-assignment1.git
cd dbms-assignment1
